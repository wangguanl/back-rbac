
## 实现质量上的缺口（方案有、代码弱）

> **更新（2026-06-28）**：以下 5 项缺口已全部处理。详见 [质量缺口-解决方案.md](./质量缺口-解决方案.md)。

这些不影响「是不是 RBAC」，但影响生产可用性：

1. ~~**登出不完整**~~ ✅ 已修复：Header 通过 `useAuth().logout()` → `clearSession()` 统一登出，含路由重置 + 401 同步清理
2. ~~**双 token 存储**~~ ✅ 已接受：Pinia persist + rbac_token 双写，通过 login/resetState 同步维护
3. ~~**Redis token 未参与校验**~~ ⚠️ 延后（开发环境跳过）：生产环境通过 `REDIS_SESSION_CHECK=true` 启用
4. ~~**日志模块**~~ ⏳ 延后：数据库表已建、record() 已就绪，HTTP 接口后续按需补齐
5. ~~**文档示例代码过时**~~ ✅ 已修复：前端方案、Phase 文档已对齐实际代码

---
